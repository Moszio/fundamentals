const header = document.getElementById(`header`)
const p = document.getElementById(`para`)
const ul = docuent.getElementById(`list-1`)

const li = document.createElement(`li`)
document.ul.append(liItem)